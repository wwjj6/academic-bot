# 📘 الدليل الشامل للمبتدئين — من الصفر حتى تشغيل البوت ورفع الكود

> هذا الدليل مكتوب لشخص **لم يسبق له** إنشاء بوت تيليجرام أو استخدام أدوات البرمجة.
> اتبع الخطوات بالترتيب، ولا تنتقل لخطوة قبل إنهاء التي قبلها. ⏱ الوقت المتوقع: 30–45 دقيقة.

---

## 🧰 الأدوات التي ستحتاجها (كلها مجانية)

| # | الأداة | ما هي؟ | رابط التحميل / الوصول |
|---|---|---|---|
| 1 | **تيليجرام** | تطبيق المراسلة الذي سيعمل عليه البوت | <https://telegram.org> |
| 2 | **BotFather** | البوت الرسمي من تيليجرام لإنشاء البوتات | <https://t.me/BotFather> |
| 3 | **userinfobot** | بوت يعطيك معرفك الرقمي في تيليجرام | <https://t.me/userinfobot> |
| 4 | **Python** | لغة البرمجة التي كُتب بها البوت | <https://www.python.org/downloads> |
| 5 | **Git** | أداة لحفظ نسخ الكود ورفعه إلى الإنترنت | <https://git-scm.com/downloads> |
| 6 | **GitHub** | موقع لتخزين الكود ومشاركته (مثل Google Drive لكن للأكواد) | <https://github.com> |
| 7 | **VS Code** *(اختياري)* | محرر لفتح ملفات الكود وتعديلها بسهولة | <https://code.visualstudio.com> |

---

# الجزء الأول: إنشاء بوت جديد في تيليجرام 🤖

### ما هو البوت أصلاً؟
البوت هو حساب تيليجرام **آلي** يرد على الرسائل بواسطة برنامج يعمل على جهازك. أنت ستنشئ "الحساب" من تيليجرام، ثم تشغّل "العقل" (كود Python) على الكمبيوتر.

### الخطوة 1: افتح BotFather
1. افتح تيليجرام
2. في خانة البحث 🔍 اكتب: `BotFather`
3. اختر النتيجة التي بجانبها **علامة التوثيق الزرقاء ✔️** (يوجد مقلدون!)
   أو ادخل مباشرة عبر الرابط: <https://t.me/BotFather>
4. اضغط زر **Start** (أو أرسل `/start`)

### الخطوة 2: أنشئ البوت
1. أرسل الأمر:
   ```
   /newbot
   ```
2. سيسألك BotFather: *"Alright, a new bot. How are we going to call it?"*
   أرسل **الاسم المعروض** للبوت — يقبل العربية والمسافات، مثال:
   ```
   المسابقات الأكاديمية - كلية الطب
   ```
3. سيسألك عن **اسم المستخدم** (username) — له شرطان:
   - أحرف إنجليزية وأرقام فقط
   - **يجب أن ينتهي بكلمة `bot`**

   مثال صحيح: `Medical_Quiz_2026_bot` ✅
   مثال خاطئ: `Medical_Quiz` ❌ (لا ينتهي بـ bot)

   > إذا رد عليك *"Sorry, this username is already taken"* فالاسم محجوز — جرب اسماً آخر.

### الخطوة 3: احفظ التوكن 🔑
عند النجاح سيرسل BotFather رسالة تهنئة تحتوي سطراً مشابهاً لهذا:
```
Use this token to access the HTTP API:
1234567890:AAAbbbCCCdddEEEfffGGGhhhIIIjjjKK
```
هذا هو **توكن البوت** — انسخه واحفظه في مكان آمن (سنستخدمه في الجزء الثالث).

> ⚠️ **تحذير مهم جداً**: من يحصل على هذا التوكن يتحكم بالبوت بالكامل!
> - لا ترسله في أي مجموعة أو محادثة
> - لا تنشره على الإنترنت
> - إذا تسرب: ارجع لـ BotFather ← `/mybots` ← اختر بوتك ← **API Token** ← **Revoke current token** — يُلغى القديم فوراً وتحصل على جديد

### الخطوة 4 (اختيارية لكن مستحسنة): جمّل البوت
في BotFather أرسل:
- `/setdescription` ← اختر بوتك ← اكتب وصفاً يظهر للمستخدم قبل الضغط على Start
- `/setabouttext` ← نبذة تظهر في صفحة البوت
- `/setuserpic` ← أرسل صورة لتصبح صورة البوت

---

# الجزء الثاني: تجهيز الكمبيوتر 💻

### الخطوة 1: ثبّت Python
1. افتح: <https://www.python.org/downloads>
2. اضغط الزر الأصفر الكبير **Download Python 3.x.x**
3. شغّل الملف الذي تم تحميله
4. ⚠️ **أهم خطوة**: في أول شاشة تثبيت، فعّل المربع في الأسفل:
   ```
   ☑ Add python.exe to PATH
   ```
   ثم اضغط **Install Now**

> 💡 إذا كان لديك **Anaconda** مثبتاً مسبقاً (<https://www.anaconda.com/download>) فلا تحتاج تثبيت Python — استخدم المسار:
> `C:\ProgramData\anaconda3\python.exe` بدل كلمة `python` في كل الأوامر القادمة.

### الخطوة 2: ثبّت Git
1. افتح: <https://git-scm.com/downloads>
2. اختر **Windows** وحمّل المثبت
3. شغّله واضغط **Next** على كل الخيارات (الإعدادات الافتراضية ممتازة)

### الخطوة 3: افتح موجه الأوامر
كل الأوامر القادمة تُكتب في نافذة سوداء اسمها "موجه الأوامر":
1. اضغط زر **Windows** في لوحة المفاتيح
2. اكتب: `cmd`
3. اضغط **Enter** — ستفتح نافذة سوداء

### الخطوة 4: تأكد أن كل شيء مثبت
اكتب في النافذة السوداء (واضغط Enter بعد كل سطر):
```bash
python --version
git --version
```
إذا ظهر رقم إصدار لكل أمر (مثل `Python 3.12.4` و `git version 2.45`) فكل شيء جاهز ✅

> ❗ إذا ظهر `'python' is not recognized...` فمعناه أنك لم تفعّل "Add to PATH" — أعد تثبيت Python مع تفعيل المربع.

---

# الجزء الثالث: إعداد كود البوت ⚙️

### الخطوة 1: احصل على الكود
**إذا كان الكود موجوداً على جهازك** (كما في حالة هذا المشروع في `d:\Claude_project\telegram_bot`) — انتقل للخطوة 2 مباشرة.

**إذا أردت تحميله من GitHub**:
```bash
cd Desktop
git clone https://github.com/hazeemcs/Telegram_bot.git
cd Telegram_bot
```
> الأمر `cd` معناه "ادخل إلى المجلد" — و `git clone` معناه "حمّل نسخة من الكود".

### الخطوة 2: ادخل إلى مجلد المشروع
```bash
cd /d d:\Claude_project\telegram_bot
```

### الخطوة 3: ثبّت مكتبات البوت
```bash
python -m pip install -r requirements.txt
```
> هذا الأمر يقرأ ملف `requirements.txt` ويثبت المكتبات التي يحتاجها البوت (أهمها مكتبة `python-telegram-bot` التي تتخاطب مع تيليجرام). انتظر حتى ينتهي (دقيقة تقريباً).

### الخطوة 4: اعرف معرفك الرقمي في تيليجرام
1. افتح في تيليجرام: <https://t.me/userinfobot>
2. اضغط **Start**
3. سيرد عليك برسالة فيها `Id: 123456789` — **هذا معرفك الرقمي**، انسخه

### الخطوة 5: أنشئ ملف الإعدادات
1. في مجلد المشروع، انسخ الملف `config.example.json` والصقه باسم جديد: `config.json`
   (أو بالأمر: `copy config.example.json config.json`)
2. افتح `config.json` بالمفكرة (كليك يمين ← Open with ← Notepad) أو بـ VS Code
3. عدّل السطور التالية:

```json
{
    "bot_token": "ضع هنا التوكن من BotFather (الجزء الأول - الخطوة 3)",
    "admin_ids": [ضع هنا معرفك الرقمي من userinfobot],
    "admin_usernames": ["اسم_مستخدم_مشرف_إضافي_بدون_@"]
}
```

مثال بعد التعديل:
```json
{
    "bot_token": "1234567890:AAAbbbCCCdddEEEfffGGG",
    "admin_ids": [987654321],
    "admin_usernames": ["Hiam_Adnan"]
}
```
4. احفظ الملف (Ctrl+S)

> ⚠️ ملف `config.json` يحتوي أسراراً — وهو محمي تلقائياً من الرفع إلى الإنترنت بفضل ملف `.gitignore` الموجود في المشروع. لا تعدل `.gitignore` ولا تحذفه.

---

# الجزء الرابع: تشغيل البوت 🚀

### الخطوة 1: شغّل البوت
في موجه الأوامر (وأنت داخل مجلد المشروع):
```bash
python bot.py
```
مع أناكوندا:
```bash
C:\ProgramData\anaconda3\python.exe bot.py
```

### الخطوة 2: تأكد من نجاح التشغيل
سترى رسائل مشابهة لهذه:
```
✅ قاعدة البيانات جاهزة.
🚀 بوت المسابقات الأكاديمية يعمل الآن...
✅ فُعّل زر القائمة (☰) بجانب حقل الكتابة.
Application started
```
🎉 مبروك! البوت يعمل الآن.

> 📌 **مهم**: البوت يعمل **ما دامت النافذة السوداء مفتوحة**. إذا أغلقتها توقف البوت.
> لإيقاف البوت يدوياً: اضغط `Ctrl + C` داخل النافذة.

### الخطوة 3: جرّب البوت
1. افتح تيليجرام وابحث عن اسم المستخدم الذي أنشأته (مثل `t.me/HU_2_levelbot`)
2. اضغط **Start**
3. يجب أن تصلك رسالة الترحيب 🏆 ثم التعليمات ثم **لوحة المشرف** (لأن معرفك في `admin_ids`)

### الخطوة 4: الإعداد الأولي داخل البوت (بالترتيب!)
1. من لوحة المشرف: **📚 إدارة المقررات** ← أرسل أسماء المقررات واحداً تلو الآخر
2. **👨‍🏫 إدارة المدرسين** ← أضف كل مدرس بمعرفه الرقمي (يحصل عليه من userinfobot ويرسله لك)
3. **🧑‍⚖️ إدارة لجنة المسابقات** ← أضف أعضاء اللجنة بنفس الطريقة
4. عضو اللجنة يدخل للبوت ← **🔗 تعيين مقررات المدرسين** ← يربط كل مدرس بمقرراته
5. الطلاب يدخلون `/start` ويسجلون أنفسهم
6. المدرس ينشئ مسابقة ← اللجنة تعتمدها ← **كل شيء بعد ذلك تلقائي** (الإشعارات، البدء، الإغلاق، النتائج)

### حل المشاكل الشائعة عند التشغيل

| الرسالة | السبب | الحل |
|---|---|---|
| `InvalidToken` | التوكن منسوخ خطأ في `config.json` | انسخه من BotFather من جديد كاملاً |
| `Conflict: terminated by other getUpdates` | البوت يعمل مرتين في نفس الوقت | أغلق كل النوافذ السوداء وشغّله مرة واحدة |
| `ModuleNotFoundError: No module named 'telegram'` | المكتبات غير مثبتة | أعد الجزء الثالث - الخطوة 3 |
| `'python' is not recognized` | لم يُفعّل Add to PATH | أعد تثبيت Python مع تفعيل المربع |

---

# الجزء الخامس: رفع الكود إلى GitHub ☁️

> لماذا؟ نسخة احتياطية من الكود على الإنترنت + إمكانية تحميله على أي جهاز آخر.

### الخطوة 1: أنشئ حساب GitHub
1. افتح: <https://github.com/signup>
2. أدخل بريدك الإلكتروني ← كلمة مرور ← اسم مستخدم ← أكمل التسجيل

### الخطوة 2: أنشئ مستودعاً (Repository)
المستودع = مجلد لمشروعك على GitHub:
1. بعد تسجيل الدخول اضغط زر **+** أعلى يمين الصفحة ← **New repository**
2. **Repository name**: اكتب اسماً مثل `Telegram_bot`
3. اختر **Private** 🔒 (خاص — لا يراه أحد غيرك) — مستحسن
4. اضغط **Create repository**
5. انسخ رابط المستودع، سيكون بالشكل:
   `https://github.com/اسم_المستخدم/Telegram_bot.git`

### الخطوة 3: أنشئ "توكن الوصول" (Personal Access Token)
GitHub لا يقبل كلمة المرور العادية من موجه الأوامر — يحتاج توكناً خاصاً:

1. افتح: <https://github.com/settings/personal-access-tokens/new>
   (أو: صورتك الشخصية ← **Settings** ← **Developer settings** ← **Fine-grained personal access tokens** ← **Generate new token**)
2. **Token name**: أي اسم، مثل `bot-upload`
3. **Expiration**: اختر 90 يوماً
4. **Repository access**: اختر **Only select repositories** ← اختر مستودعك `Telegram_bot`
5. تحت **Permissions** ← **Repository permissions** ← ابحث عن **Contents** ← غيّرها إلى:
   ```
   Contents: Read and write
   ```
   > ⚠️ هذه الخطوة **إلزامية** — بدونها سيفشل الرفع بخطأ `403: Write access not granted`
6. اضغط **Generate token** وانسخ التوكن فوراً (يظهر مرة واحدة فقط!) — يبدأ بـ `github_pat_`

### الخطوة 4: ارفع الكود
في موجه الأوامر داخل مجلد المشروع، نفّذ الأوامر التالية بالترتيب:

```bash
git init -b main
```
> ينشئ "سجل نسخ" للمشروع (مرة واحدة فقط — إن كان المشروع مهيأً مسبقاً سيقول موجود بالفعل، لا مشكلة)

```bash
git config user.name "اسمك"
git config user.email "بريدك@example.com"
```
> يعرّف Git من أنت (يظهر اسمك على كل نسخة تحفظها)

```bash
git add -A
git status
```
> `add -A` يجهز كل الملفات للحفظ، و `status` يعرض القائمة —
> ✅ **تأكد أن `config.json` و `API_Token` لا يظهران في القائمة** (محميان تلقائياً)

```bash
git commit -m "رفع كود بوت المسابقات"
```
> يحفظ نسخة من الكود مع وصف

```bash
git remote add origin https://github.com/اسم_المستخدم/Telegram_bot.git
git push -u origin main
```
> `remote add` يربط مجلدك بالمستودع على GitHub، و `push` يرفع الكود.
> عند طلب اسم المستخدم: اكتب اسم مستخدم GitHub.
> عند طلب **Password**: الصق **التوكن** الذي أنشأته في الخطوة 3 (وليس كلمة المرور!)
> ملاحظة: عند لصق التوكن لن يظهر على الشاشة — هذا طبيعي، الصقه واضغط Enter.

### الخطوة 5: تأكد من نجاح الرفع
افتح صفحة مستودعك في المتصفح:
`https://github.com/اسم_المستخدم/Telegram_bot`
سترى ملفات المشروع، وملف `README.md` يظهر تلقائياً كصفحة تعريف 🎉

### التحديثات المستقبلية
كلما عدّلت الكود وأردت رفع التعديل:
```bash
git add -A
git commit -m "وصف قصير للتعديل"
git push
```

---

# 🔐 تذكير أمني أخير

| ملف/معلومة | القاعدة |
|---|---|
| توكن البوت (من BotFather) | في `config.json` فقط — لا يُرفع ولا يُرسل لأحد |
| توكن GitHub (`github_pat_...`) | يُستخدم عند الرفع فقط — إن تسرب احذفه من إعدادات GitHub وأنشئ غيره |
| `config.json` و `academic_bot.db` | يبقيان على جهازك — `.gitignore` يمنع رفعهما تلقائياً |
| تسرب توكن البوت؟ | BotFather ← `/mybots` ← بوتك ← API Token ← **Revoke** ← ضع الجديد في `config.json` |

> 📮 إذا واجهت أي مشكلة، تواصل عبر بوت التواصل الخاص بالدفعة.

**بالتوفيق! 🍀**
