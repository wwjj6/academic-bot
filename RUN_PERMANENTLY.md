# ♾️ تشغيل البوت بشكل دائم (على مدار الساعة)

هذا الدليل يشرح كيف تجعل البوت يعمل باستمرار — يبدأ تلقائياً، ويستمر بعد إغلاق النوافذ، ويُعيد تشغيل نفسه إذا توقّف.

> ⚠️ **قاعدة ذهبية**: لا تشغّل البوت بأكثر من طريقة في وقت واحد. تشغيل نسختين معاً يسبب تعارضاً في تيليجرام (خطأ 409). اختر **طريقة واحدة** فقط من الأسفل، وأوقف أي تشغيل يدوي أو من لوحة التحكم قبلها.

---

## مقارنة سريعة بين الطرق

| الطريقة | يعمل بعد إعادة تشغيل الجهاز | يعمل بلا تسجيل دخول | إعادة تشغيل تلقائية | يحتاج تحميل | الأنسب لـ |
|---|:---:|:---:|:---:|:---:|---|
| **1. المهمة المجدولة** (موصى بها) | ✅ عند الدخول | ❌ | ✅ | ❌ | معظم المستخدمين |
| **2. NSSM كخدمة ويندوز** | ✅ عند الإقلاع | ✅ | ✅ | ✅ (ملف واحد) | خادم يعمل 24/7 |
| **3. مجلد بدء التشغيل** | ✅ عند الدخول | ❌ | ✅ | ❌ | الأبسط |

---

## ✅ الطريقة 1: المهمة المجدولة (Task Scheduler) — موصى بها

الأسهل والأقوى بلا أي تحميل. يعمل البوت تلقائياً عند تسجيل دخولك، ويُعاد تشغيله إذا توقّف.

### الخطوات
1. افتح مجلد `service` داخل المشروع
2. انقر نقراً مزدوجاً على: **`install_bot_task.bat`**
3. سيسجّل المهمة ويشغّل البوت فوراً، وستظهر رسالة نجاح خضراء

هذا كل شيء! البوت الآن يعمل دائماً.

### للتأكد أنه يعمل
افتح **لوحة التحكم** (`admin_panel\run_panel.bat`) — ستجد حالة البوت 🟢 يعمل.
أو من موجّه الأوامر:
```
schtasks /Query /TN "TelegramAcademicBot"
```

### لإيقاف التشغيل الدائم نهائياً
انقر نقراً مزدوجاً على: **`service\uninstall_bot_task.bat`**

### ملاحظات
- يعمل البوت بلا نافذة مرئية (عبر `pythonw.exe`)
- عند توقّف البوت لأي سبب، تعيد المهمة تشغيله خلال دقيقة
- يبدأ تلقائياً عند كل تسجيل دخول لجهازك

---

## 🖥 الطريقة 2: NSSM كخدمة ويندوز (للتشغيل 24/7 بلا تسجيل دخول)

الأقوى للخوادم: يعمل البوت كـ«خدمة ويندوز» تبدأ عند إقلاع الجهاز حتى قبل تسجيل الدخول، مع إشراف كامل وإعادة تشغيل.

### الخطوات
1. حمّل NSSM من: <https://nssm.cc/download> (اختر أحدث إصدار، ثم مجلد `win64`)
2. فُكّ الضغط وضع `nssm.exe` في مكان ثابت، مثلاً `C:\nssm\nssm.exe`
3. افتح **موجّه الأوامر كمسؤول** (Run as administrator)
4. نفّذ:
   ```
   C:\nssm\nssm.exe install TelegramAcademicBot "C:\ProgramData\anaconda3\pythonw.exe" "d:\Claude_project\telegram_bot\bot.py"
   C:\nssm\nssm.exe set TelegramAcademicBot AppDirectory "d:\Claude_project\telegram_bot"
   C:\nssm\nssm.exe set TelegramAcademicBot AppStdout "d:\Claude_project\telegram_bot\bot_service.log"
   C:\nssm\nssm.exe set TelegramAcademicBot AppStderr "d:\Claude_project\telegram_bot\bot_service.log"
   C:\nssm\nssm.exe set TelegramAcademicBot Start SERVICE_AUTO_START
   C:\nssm\nssm.exe start TelegramAcademicBot
   ```

### التحكم بالخدمة لاحقاً
```
C:\nssm\nssm.exe stop TelegramAcademicBot       :: إيقاف
C:\nssm\nssm.exe restart TelegramAcademicBot    :: إعادة تشغيل
C:\nssm\nssm.exe remove TelegramAcademicBot confirm   :: حذف الخدمة
```

> 💡 NSSM يعيد تشغيل البوت تلقائياً عند أي توقّف، ويشتغل حتى لو لم يسجّل أحد الدخول — مثالي لجهاز يعمل باستمرار.

---

## 📂 الطريقة 3: مجلد بدء التشغيل (الأبسط)

يشغّل ملف إعادة تشغيل تلقائية عند تسجيل دخولك. تظهر نافذة سوداء صغيرة (يمكن تصغيرها).

### الخطوات
1. اضغط `Win + R`، اكتب `shell:startup` ثم Enter — سيفتح مجلد بدء التشغيل
2. انسخ اختصاراً لملف: `service\run_bot_forever.bat` والصقه في هذا المجلد
   (كليك يمين على الملف ← إرسال إلى ← سطح المكتب، ثم انقل الاختصار إلى مجلد بدء التشغيل)
3. جاهز — سيعمل البوت عند كل تسجيل دخول، ويُعاد تشغيله تلقائياً إذا توقّف

> يكتب هذا الملف السجل في `bot_service.log` داخل مجلد المشروع.

---

## 📜 متابعة السجل

في كل الطرق، يُكتب سجل البوت في ملف `bot_service.log` داخل مجلد المشروع.
لعرض آخر الأسطر مباشرة من موجّه أوامر PowerShell:
```
Get-Content "d:\Claude_project\telegram_bot\bot_service.log" -Tail 30 -Wait
```
أو تابع الحالة والسجل حيّاً من **لوحة التحكم** (الصفحة الرئيسية).

---

## ❓ حل المشاكل

| المشكلة | الحل |
|---|---|
| `Conflict: terminated by other getUpdates` | البوت يعمل بأكثر من طريقة. أوقف الكل وأبقِ طريقة واحدة (انظر القاعدة الذهبية أعلى) |
| البوت لا يبدأ بعد إعادة التشغيل | الطريقتان 1 و3 تحتاجان **تسجيل الدخول**. للعمل بلا دخول استخدم NSSM (الطريقة 2) |
| لا يظهر سجل | تأكد أنك تستخدم الطريقة التي تكتب `bot_service.log`؛ الطريقة 1 عبر pythonw لا تكتب سجلاً — عدّلها لاستخدام الطريقة 3 إن أردت سجلاً مفصّلاً |
| «توكن غير مضبوط» في السجل | اضبط التوكن من لوحة التحكم ← إعدادات البوت |

---

## ⚠️ تنبيه مهم بخصوص لوحة التحكم

زر «تشغيل/إيقاف» في لوحة التحكم مخصّص للتشغيل **اليدوي والتجريبي**. إذا فعّلت التشغيل الدائم (أي طريقة أعلاه)، فلا تستخدم زر التشغيل في اللوحة أيضاً حتى لا تعمل نسختان. أما زر **الإيقاف** في اللوحة فيوقف النسخة الحالية، لكن الطريقة 1 و2 ستعيد تشغيلها تلقائياً خلال دقيقة — وهذا هو المقصود من «الدائم».
